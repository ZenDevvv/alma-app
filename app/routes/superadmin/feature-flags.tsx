import { ComingSoon } from "@/components/coming-soon";

export default function FeatureFlagsPage() {
	return (
		<ComingSoon
			title="Feature Flags"
			description="Enable or disable platform features per organization and manage feature rollouts."
		/>
	);
}
