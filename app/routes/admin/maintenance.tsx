import { ComingSoon } from "@/components/coming-soon";

export default function MaintenancePage() {
	return (
		<ComingSoon
			title="Maintenance"
			description="Configure system maintenance mode, schedule downtime windows, and manage user notifications during maintenance."
		/>
	);
}
