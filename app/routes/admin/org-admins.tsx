import { ComingSoon } from "@/components/coming-soon";

export default function OrgAdminsPage() {
	return (
		<ComingSoon
			title="Organization Admins"
			description="Manage organization administrators, assign roles, and configure granular permissions across all organizations."
		/>
	);
}
