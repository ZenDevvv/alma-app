import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
	return (
		<ComingSoon
			title="Settings"
			description="Configure global platform settings including default language, timezone, academic year format, and other system-wide preferences."
		/>
	);
}
